# x402 Integration

Platform-internal feature — not required for MCP skill users.

Noelclaw account holders authenticate with `NOELCLAW_API_KEY` and never pay per-call. Set it in your MCP client config's `env` block.
